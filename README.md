#Random Quote Generator
Displays a famous/not so famous quote by a known person.
The generator fetches quotes from a website ('https://zenquotes.io/api/random')
Built in Python to use in terminal environments.

1.Clone the repository
2.Install/Import:
-requests
-paperclip
3.Run the generator
